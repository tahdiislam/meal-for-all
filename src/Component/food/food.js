import React from 'react';

const Food = () => {
    return (
        <div>
            <input type="text" />
            <button type='button'>Search</button>
        </div>
    );
};

export default Food;